# Copy this file to .env and fill in real values.
# NEVER commit the real .env file — it's already in .gitignore below.

MQTT_HOST=your-cluster-id.s1.eu.hivemq.cloud
MQTT_PORT=8883
MQTT_USERNAME=PrintPulse
MQTT_PASSWORD=your-rotated-password-here
